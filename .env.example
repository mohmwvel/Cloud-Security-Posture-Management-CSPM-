# Example Environment Variables
# Do not place real AWS credentials here; use IAM roles or aws configure natively.

FLASK_APP=run.py
FLASK_ENV=development
FLASK_SECRET_KEY=replace-this-with-a-secure-random-key

# Rate Limiting
RATELIMIT_DEFAULT=50/minute
RATELIMIT_STORAGE_URI=memory://
